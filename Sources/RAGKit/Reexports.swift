// VecturaKit types (VecturaKit, VecturaSearchResult, VecturaConfig, …) are
// part of RAGKit's public API surface, so re-export the module rather than
// forcing hosts to declare a second package dependency.
@_exported import VecturaKit
